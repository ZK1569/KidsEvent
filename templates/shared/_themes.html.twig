{# ---- Box slider -------------- #}
<div class="all-slider">

    

    <div class="box">
        {# ---- img-box ----------- #}
        <div class="slider-img">
        
            <img src="{{asset('img/Themes/' ~ product.mainPicture)}}" alt="{{product.name}}">
            {# --- Overlayer ------------ #}
            <div class="overlay">
                <a href="{{url ('product_show', {'slug':product.slug} )}}" class="buy-btn">Plus d'info</a>
            </div>
        </div>

        {# --- detail-box ------ #}
        <div class="detail-box">
            {# --- type --------- #}
            <div class="type">
                <p href="#">{{product.name | upper}}</p>
            </div>
            {# --- price ---------#}
            <a href="#" class="price">{{product.price}} €</a>


        </div>
    </div>
</div> 